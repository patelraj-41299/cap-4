version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20
    commands:
      - echo Installing dependencies
  build:
    commands:
      - echo Build phase complete

artifacts:
  files:
    - app/**/*     # includes appspec.yml + scripts
